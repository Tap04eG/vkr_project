const http = require('http'); const req = http.request({hostname: 'localhost', port: 8000, path: '/auth/token', method: 'POST', headers: {'Content-Type': 'application/x-www-form-urlencoded'}}, (res) => { let data = ''; res.on('data', d => data += d); res.on('end', () => { const token = JSON.parse(data).access_token; http.get('http://localhost:8000/tasks/my', {headers: {'Authorization': 'Bearer ' + token}}, (res2) => { let data2 = ''; res2.on('data', d => data2 += d); res2.on('end', () => console.log(JSON.parse(data2).map(t => t.task_type))); }); }); }); req.write('username=student1&password=password1'); req.end();
